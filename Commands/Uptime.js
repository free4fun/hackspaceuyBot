class Uptime {
  constructor(ircBot) {
 /*
 var sys = require('sys')
  var exec = require('child_process').exec;
  exec("uptime", function(error, stdout, stderr) {
   nodeIrc.say(chanName, stdout);
   tg(stdout)
     }*/
 }
 //nodeIrc.say(chanName, '!uptime');

}

module.exports = Uptime;
